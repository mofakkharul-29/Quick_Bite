import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFFFF6B00);
  static const primaryLight = Color(0xFFFF8A3D);
  static const primaryDark = Color(0xFFE55A00);

  // Backgrounds
  static const background = Color(0xFFFFF7ED);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFFFF3E8);

  // Text
  static const textPrimary = Color(0xFF1F2937);
  static const textSecondary = Color(0xFF6B7280);

  // Borders
  static const border = Color(0xFFE5E7EB);

  // States
  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);

  // Misc
  static const divider = Color(0xFFF3F4F6);
  static const shadow = Color(0x14000000);
}
